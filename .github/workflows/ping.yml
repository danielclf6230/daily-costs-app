name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *" # Every 5 minutes UTC
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to Render
        run: curl -s https://daily-costs-app.onrender.com
