{
  "name": "trip-tools-app",
  "private": true,
  "scripts": {
    "start": "npm --prefix daily-cost-frontend start",
    "dev": "npm --prefix daily-cost-frontend dev",
    "build": "npm --prefix daily-cost-frontend build"
  }
}
